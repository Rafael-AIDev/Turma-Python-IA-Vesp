
while True:
    # entrada de dados
    x = str(input('Digite um número: ')).replace(',', '.')
    y = str(input('Digite outro número: ')).replace(',', '.')

    # conversão
    x = float(x)
    y = float(y)

    multiplicacao = x * y
    soma = x + y
    subtracao = x - y
    divisao = x / y

    